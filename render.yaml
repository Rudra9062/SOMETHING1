services:
  - type: worker
    name: instagram-group-bot
    env: python
    region: oregon
    plan: free
    branch: main
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
